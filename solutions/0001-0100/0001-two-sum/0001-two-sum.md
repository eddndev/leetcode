# Two Sum

## Problem Description
Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.

## Approach
Using a hash map to store the complement of the current number.

## Complexity
- **Time**: $O(n)$
- **Space**: $O(n)$
